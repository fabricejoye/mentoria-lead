package com.lead.pizzaria.repositories;

import com.lead.pizzaria.entities.Pizza_extra;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PizzaExtraRepository extends JpaRepository<Pizza_extra,Long> {
}
